import { ConvertMilleToKmPipe } from './convert-mille-to-km.pipe';

describe('ConvertMilleToKmPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMilleToKmPipe();
    expect(pipe).toBeTruthy();
  });
});
